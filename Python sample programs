(1)
# Write a python program to read the sides ofa triangle a, b and calculate the hypotenue using the formula square root(square of a + square of b) 
a = float(input())
b = float(input())
c = (a**2 + b**2)**0.5
print(c)

(2)
# Write a python program to read a string and use appropriate string operation to print the string 20 times in a line with no space
str1= input()
str2= str1*20
print(str2)

(3)
# Write a python program to read a string and find the number of vowels and consonants in the string
str1=input("Enter the string")
countV=0
countC=0
for ch in str1:
  for i in 'aeiouAEIOU':
    if (ch==i):
      countV+=1
  if (ch not in 'aeiouAEIOU'):
    countC+=1
print("The number of vowels present in the string are",countV)
print("The number of conconants present in the string are ",countC

(4)
# Using 'while' write a Python program to print the multiplication table of n, 2<=n<=10. Replace the 'while' statement with an equivalent 'for'  statement to get the same output
n=int(input())
i=1
#using while loop
if(n>=2 and n<=10):
  while i<=10:
    print(n,"x",i,"=",n*i)
    i=i+1

#using for loop
if(n>=2 and n<=10):
  for i in range(1,11):
    print(n,"*",i,"=",n*i)

(5)
# Write a Python program to create a list of strings. Separate palindromes in the list to another list using reverse() function


def reverse(s):
  r = ''.join(reversed(s))
  if s == r:
    l1.append(s)
    
l = eval(input())
l1 = []
for i in range(len(l)):
  reverse(l[i])
print(l1)

 



